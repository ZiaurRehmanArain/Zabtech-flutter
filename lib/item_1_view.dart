import 'package:flutter/material.dart';

class Item1View extends StatelessWidget {
  const Item1View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Item 1'),),
    );
  }
}